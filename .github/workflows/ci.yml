name: ci

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    # - name: Lint
    #   run: |
    #     make lint

    - name: Install
      run: |
        make install

    # - name: Test
    #   env:
    #     CI: true
    #   run: |
    #     make test
